---
import { Image } from "astro:assets";
import { ArrowRight, MultipleImage } from "../assets";
---

<div class="mt-32 h-screen px-14 w-full flex justify-between items-center">
  <div class="ml-8">
    <div class="flex flex-col gap-8">
      <h1 class="text-4xl font-bold">Centralized</h1>
      <h1 class="text-4xl font-bold text-blue-600">Data Repository</h1>
    </div>
    <div class="w-1/2 my-5">
      <p class="text-zinc-600 font-bold text-lg">
        We specialize in delivering high-quality global databases and targeted
        lead generation systems, designed to empower businesses with the
        insights necessary to make informed, high-impact decisions.
      </p>
    </div>
    <div class="my-8 flex flex-col items-start gap-4">
      <button class="btn text-teal-600 text-lg"
        ><Image
          src={ArrowRight}
          alt={"arrow right"}
          height={20}
          width={20}
        />Enterprise-Grade Data Solutions</button
      >
      <button class="btn text-teal-600 text-lg"
        ><Image
          src={ArrowRight}
          alt={"arrow right"}
          height={20}
          width={20}
        />Advanced Data Solutions</button
      >
      <button class="btn text-teal-600 text-lg"
        ><Image
          src={ArrowRight}
          alt={"arrow right"}
          height={20}
          width={20}
        />Consulting & Insght Generation</button
      >
    </div>

    <button class="btn btn-primary w-48 rounded-2xl text-lg"
      >Get Free Data</button
    >
  </div>

  <div class="w-1/2 object-contain">
    <Image src={MultipleImage} alt={"multiple image"} />
  </div>
</div>
