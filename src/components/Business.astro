---
import { Image } from "astro:assets";
import { ImageSeries, ArrowRight } from "../assets";
---

<div class="mt-32 h-screen px-14 w-full flex justify-between items-center">
  <div class="w-1/2 ml-8">
    <div class="flex flex-col">
      <h1 class="text-black text-4xl font-extrabold">Empower your</h1>
      <div class="my-4"></div>
      <h2 class="text-4xl text-blue-800 font-extrabold">Business</h2>
    </div>

    <div class="my-5 flex flex-col items-start gap-10">
      <h3 class="text-black text-xl font-semibold">
        Let us guide you in unleashing the full <div class="my-1"></div> capabilities
        of your product or service.
      </h3>
      <h3 class="text-black text-lg">
        With our deep expertise, we’ll help you navigate <div class="my-1">
        </div> your path to success more swiftly and intelligently by
      </h3>
    </div>

    <div class="my-5 flex flex-col items-start gap-4">
      <button class="btn text-teal-600"
        ><Image src={ArrowRight} alt={"arrow right"} height={20} width={20} /> Sustainable
        scalability</button
      >
      <button class="btn text-teal-600"
        ><Image src={ArrowRight} alt={"arrow right"} height={20} width={20} /> Operational
        excellence</button
      >
      <button class="btn text-teal-600"
        ><Image src={ArrowRight} alt={"arrow right"} height={20} width={20} /> Market
        competitiveness</button
      >
    </div>

    <button class="btn btn-primary w-40 rounded-2xl">Free Guide</button>
  </div>
  <div class="w-1/3">
    <Image src={ImageSeries} alt={"images"} />
  </div>
</div>
